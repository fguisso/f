# API

Express server using TypeScript and Prisma. Configure the `DATABASE_URL` in an `.env` file then run `npm install` and `npm run dev`.

You can also run this API through Docker Compose from the project root.
