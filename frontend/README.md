# Frontend

This React application uses Vite and Material UI. Run `npm install` and `npm run dev` to start the development server.

You can also run this frontend through Docker Compose from the project root.
